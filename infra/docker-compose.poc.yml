services:
  web:
    image: ghcr.io/${GITHUB_REPOSITORY}/folio-web:latest
    container_name: folio-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      NODE_ENV: production
